# Backlog Quick Add 導入手順

Chrome の拡張機能で、ページ上のテキストを選択してサイドパネルから Backlog にタスクを素早く登録できるツールです。  
現在テスト運用中のため、使用中に気づいた点があればフィードバックをお願いします。

---

## 1. 拡張機能のインストール

### 1-1. ファイルの取得

共有された ZIP ファイルをダウンロードし、任意の場所に展開してください。  
※ 展開先のフォルダは削除・移動しないでください（Chrome が参照し続けます）。

### 1-2. Chrome に読み込む

1. Chrome で `chrome://extensions/` を開く
2. 右上の **「デベロッパーモード」** をオンにする
3. **「パッケージ化されていない拡張機能を読み込む」** をクリック
4. 展開したフォルダ（`manifest.json` があるフォルダ）を選択する

読み込みが完了すると、ツールバーに Backlog Quick Add のアイコンが表示されます。

> **ヒント**: アイコンが見当たらない場合は、ツールバー右端のパズルピースアイコン（拡張機能メニュー）を開き、Backlog Quick Add をピン留めしてください。

---

## 2. Backlog API キーの取得

この拡張機能は Backlog API を使用するため、各自の API キーが必要です。

1. ブラウザで [https://itreatinc.backlog.com](https://itreatinc.backlog.com) にログインする
2. 右上の自分のアイコンをクリック → **「個人設定」** を開く
3. 左メニューの **「API」** をクリック
4. 「メモ」に任意の名前（例: `Chrome拡張`）を入力し、 **「登録」** をクリック
5. 生成された API キーをコピーする

> **注意**: API キーは一度しか表示されません。コピーし忘れた場合は再発行してください。

---

## 3. 拡張機能の初期設定

1. ツールバーの Backlog Quick Add アイコンをクリックしてサイドパネルを開く
2. 初期設定画面が表示される

### 3-1. スペースID

- **スペースID** に `itreatinc` と入力する
- **ドメイン** は `backlog.com` を選択する（デフォルトのまま）

### 3-2. API キー

- 手順 2 で取得した API キーを貼り付ける

### 3-3. 保存

- **「保存」** をクリックする
- 接続に成功するとプロジェクト一覧が読み込まれ、タスク作成フォームが表示されます

---

## 4. 基本的な使い方

### タスクの作成

1. 任意の Web ページで、タスクにしたいテキストを選択する
2. 以下のいずれかの方法でフォームを開く
   - 選択範囲を **右クリック** → **「Backlog にタスク作成」**
   - ツールバーの **拡張機能アイコン** をクリック
   - キーボードショートカット: `Ctrl+Shift+B`（Mac: `Command+Shift+B`）
3. サイドパネルで **プロジェクト・課題種別・概要・詳細・担当者・期限** などを入力
4. **「送信」** をクリックして課題を登録

選択したテキストは自動的に「詳細」欄に反映されます。

### 設定の変更

サイドパネル内の **「設定」** リンクから、以下のオプションを変更できます。

- 送信後に作成した課題の Backlog ページを自動で開く
- ポップパネル（小窓）モードで開く（Arc ブラウザ推奨）
- API キーの変更
- プロジェクト・担当者情報の手動更新

---

## 5. トラブルシューティング

| 症状 | 対処 |
|------|------|
| アイコンをクリックしても何も起きない | `chrome://extensions/` で拡張機能が有効になっているか確認 |
| 「APIキーが無効です」と表示される | API キーを再確認。コピーミスがないか確認し、必要なら Backlog で再発行 |
| プロジェクトが表示されない | 設定画面から「プロジェクト・担当者情報の更新」を実行 |
| 選択テキストが反映されない | ページをリロードしてから再度テキストを選択してみる |

---

## 6. アンインストール

`chrome://extensions/` を開き、Backlog Quick Add の **「削除」** をクリックしてください。  
展開したフォルダも不要であれば削除して構いません。

---

**バージョン**: 0.0.1（テスト版）  
不具合やご要望があればお気軽にご連絡ください。
